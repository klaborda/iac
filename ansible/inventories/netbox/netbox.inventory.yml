plugin: netbox.netbox.nb_inventory
api_endpoint: https://netbox.local.mafyuh.dev
validate_certs: true
config_context: false
group_by:
  - device_roles
query_filters:
  - tag: ansible
# device_query_filters:
#   - has_primary_ip: 'true'
#   - tenant__n: internal
token: "{{ lookup('pipe', 'bw get item e379959b-fa31-48aa-b37a-b2ce004d36ad | jq -r \".login.password // .fields[0].value // .notes // empty\"') }}"
compose:
  ansible_port: 2424
